# Suppress deprecated warnings from transitive dependencies during install
# These warnings are from old transitive dependencies and don't affect functionality
audit-level=moderate
loglevel=error
