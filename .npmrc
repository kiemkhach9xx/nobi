# Suppress deprecated warnings from transitive dependencies
# These warnings are from ESLint 8 and its dependencies, which are still functional
# To fully resolve, would need to migrate to ESLint 9 (breaking change)
loglevel=warn
